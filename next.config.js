/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.google.com", "uwswpa.org", "icons.veryicon.com"],
  },
};

module.exports = nextConfig;
